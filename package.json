{
  "name": "foundry-packages",
  "private": true,
  "type": "module",
  "description": "Workspace root. Not published \u2014 see packages/ for the published packages.",
  "license": "MIT",
  "workspaces": [
    "packages/*"
  ],
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "setup": "npm ci",
    "//shell": "npm runs these with /bin/sh, which is dash on most Linux CI images and has no arrays and no shopt. They must be POSIX. An unmatched glob expands to the literal pattern, so `[ -d ]` on the first word is the portable way to ask whether packages/ has anything in it.",
    "//build-order": "npm --workspaces builds in filesystem-discovery order, not dependency order. Build the dependency roots first: controller has no first-party dependency of its own and is what builder, inspector and ledger now resolve through; ui must exist before surface compiles its web templates and token consumers; and locksmith has an internal subpath that must build on a fresh clone. The governance and policy stubs that issue #282 introduced as forwarding shims are gone -- their only remaining consumers were repointed at controller directly, because a stub kept for its own sake is the debt decision 9 exists to remove.",
    "build": "set -- packages/*/; [ -d \"$1\" ] || exit 0; npm run build --workspace=packages/controller --if-present && npm run build --workspace=packages/ui --if-present && npm run build --workspace=packages/designer --if-present && npm run build --workspace=packages/writer --if-present && npm run build --workspace=packages/locksmith --if-present && npm run build --workspaces --if-present",
    "test": "set -- packages/*/; [ -d \"$1\" ] || exit 0; npm run test --workspaces --if-present",
    "typecheck": "set -- packages/*/; [ -d \"$1\" ] || exit 0; npm run typecheck --workspaces --if-present",
    "check:safety": "node scripts/check-public-safety.mjs . --allow-changelogs",
    "check:safety:strict": "node scripts/check-public-safety.mjs . --allow-changelogs --require-denylist",
    "check:scope": "node scripts/set-scope.mjs --check",
    "//check-registry": "Does every non-private packages/*/package.json's publishConfig.registry still match the single declared registry in package-scope.json? Same single-source-of-truth pattern as check:scope, extended from the scope to the registry -- see scripts/set-registry.mjs's own header and docs/PUBLISHING.md's canonical registry qualification section.",
    "check:registry": "node scripts/set-registry.mjs --check",
    "//check-prepublish-hook": "Does every non-private packages/*/package.json's scripts.prepublishOnly still run check-name-collision.mjs before npm run build? Same single-source-of-truth pattern as check:scope and check:registry above -- see scripts/set-prepublish-hook.mjs's own header for why this closes issue #273's hand-run `npm publish` bypass, and why it does not (and cannot) claim to close every bypass.",
    "check:prepublish-hook": "node scripts/set-prepublish-hook.mjs --check",
    "check:denylist": "node scripts/check-denylist-quality.mjs",
    "//check-foreign-references": "The inverse of check:safety. That gate is a denylist and can only refuse names someone already listed; this one allows only THIS repository's own account and scope (derived from package-scope.json and the manifests' own repository.url) and fails every other account-shaped reference by shape. Foundry is a neutral supplier: it must never name a consumer, a sibling repository, or any other account. See the script's own header for the leak that got past check:safety and motivated it.",
    "check:foreign-references": "node scripts/check-foreign-references.mjs",
    "//check-workspace-links": "Does every first-party 0.x dependency range still cover its sibling's real on-disk version, and does package-lock.json still resolve every first-party package as a local link rather than a remote registry URL? See the script's own header for the defect this exists to catch -- it has already cost real time more than once.",
    "check:workspace-links": "node scripts/check-workspace-links.mjs",
    "check:gates": "node scripts/test-gates.mjs && node --test scripts/agent-cloud.test.mjs scripts/deprecate-legacy-packages.test.mjs scripts/select-publishable-packages.test.mjs scripts/check-release-readiness.test.mjs scripts/check-workspace-links.test.mjs scripts/check-package-visibility.test.mjs scripts/check-package-programs.test.mjs scripts/check-workflow-references.test.mjs scripts/registry-version-lookup.test.mjs scripts/check-registry-parity.test.mjs",
    "//check-visibility-declarations": "The OFFLINE half of check:package-visibility, split out so it can run on every pull request. That gate calls the live GitHub Packages API and needs GH_PACKAGES_TOKEN, so it only runs post-publish -- which makes it a detector, not a gate: it reports a fault that has already shipped. The join between package-lifecycle.json's \"published\" entries and package-visibility.json needs no credential at all, and a missing declaration there is exactly what took four consecutive publish runs red for a data omission knowable at review time. This half is a real gate; the live half stays where the credential is.",
    "check:package-programs": "node scripts/check-package-programs.mjs",
    "check:visibility-declarations": "node scripts/check-package-visibility.mjs --declarations-only",
    "check:root-entry": "node scripts/check-root-entry-policy.mjs",
    "check:readme": "node scripts/check-root-readme-parity.mjs . && for manifest in packages/*/package.json; do [ -f \"$manifest\" ] || continue; p=${manifest%/package.json}; node scripts/check-readme-parity.mjs \"$p\" || exit 1; done",
    "check:contamination": "for manifest in packages/*/package.json; do [ -f \"$manifest\" ] || continue; p=${manifest%/package.json}; node scripts/check-contamination-classes.mjs \"$p\" || exit 1; done",
    "check:typechecked-assertions": "for manifest in packages/*/package.json; do [ -f \"$manifest\" ] || continue; p=${manifest%/package.json}; node scripts/check-typechecked-assertions.mjs \"$p\" || exit 1; done",
    "check:package-governance": "node packages/controller/dist/cli.js docs/contracts/package-lifecycle.json . --scope @vespeneventures",
    "//check-repository-profile": "Foundry supplies the repository-requirements contract and, per issue #317, declares and verifies its own governance/repository-profile.json rather than being the one repository that skips it. Invoked with `.` (the repository root), not an explicit file path, so this also exercises the packaged discovery repository-check gained in issue #315: it must locate governance/repository-profile.json itself, and a declaration anywhere else would surface as a distinct, non-canonical-location finding rather than a silent pass. `bin.js`, not `cli.js`, the same executable-only entry point `repository-check` installs as -- controller/repository's own CLI module is deliberately inert on import (see its own header), so only bin.js self-invokes.",
    "check:repository-profile": "node packages/controller/dist/repository/bin.js .",
    "//check-contrast": "ui-contrast-check ships as an installed `bin` (packages/ui/src/tokens/contrast-cli.ts), but a gate nothing ever runs is decorative \u2014 invoked here directly against ui's own compiled dist/tokens/contrast-cli.js, the same way check:package-governance below invokes controller's own dist/cli.js (packages/controller \u2014 issue #282 moved this off packages/governance/dist/cli.js, which is now just a deprecated forwarding stub), rather than via `npx` or a workspace script indirection. Needs `npm run build` (ui's own `dist/` is gitignored) to have run first, same as check:package-governance.",
    "check:contrast": "node packages/ui/dist/tokens/contrast-cli.js",
    "//check-neutrality": "The structural half of publish safety, for material meant to be shared between parties who do not govern each other: an absolute path or a named home directory discloses one operator's machine layout without containing a single denylisted string, so check:safety can never see it. Runs the BUILT @vespeneventures/controller/conventions scan (issue #282 moved this off the now-retired @vespeneventures/conventions) rather than a second copy of those regexes here \u2014 hence its position after build, alongside check:package-governance and check:contrast.",
    "check:neutrality": "node scripts/check-neutrality.mjs",
    "//check-release-readiness": "Did a package's shipped content change since its version last did, scoped to the diff against origin/main (or local main)? Offline and hermetic (see scripts/check-release-readiness.test.mjs) \u2014 no network call, just local git refs \u2014 but deliberately NOT part of `check` below: its answer is only as fresh as the contributor's local origin/main, which `git fetch` keeps current but `check`'s other gates never depend on being current for anything. Run explicitly, or trust CI's `release-readiness` job, which passes the pull request's real base explicitly rather than relying on a local ref at all. A separate `--audit` mode also exists for whole-repo, non-diff-scoped investigation \u2014 see the script's own header for why that mode is a heuristic and is not run in CI.",
    "check:release-readiness": "node scripts/check-release-readiness.mjs",
    "//check-builds-first": "build runs before typecheck for the same reason ci.yml does it in that order: dist/ is gitignored, so on a fresh clone a package's internal dependency has no .d.ts until it has been compiled. The explicit pre-build list exists because `npm run build --workspaces` iterates alphabetically, not topologically: a package that sorts BEFORE a first-party dependency compiles against declarations that do not exist yet. ui is listed because surface sorts before it; designer and writer are listed because publisher sorts before writer. This list must grow whenever a new package sorts before something it depends on, and a local tree with stale dist/ output will not reveal the omission -- only a fresh clone will.",
    "check": "npm run check:root-entry && npm run check:visibility-declarations && npm run check:package-programs && npm run check:scope && npm run check:registry && npm run check:prepublish-hook && npm run check:workspace-links && npm run check:gates && npm run check:safety && npm run check:foreign-references && npm run check:readme && npm run check:contamination && npm run check:typechecked-assertions && npm run build && npm run check:package-governance && npm run check:repository-profile && npm run check:contrast && npm run check:neutrality && npm run typecheck && npm run test",
    "agent:cloud:bootstrap": "node scripts/agent-cloud.mjs bootstrap",
    "agent:cloud:check": "node scripts/agent-cloud.mjs check",
    "//preflight": "Everything a package must pass before a publish is even proposed. Unlike `check`, this scans the packed TARBALL and asks the registry whether the name is already taken.",
    "preflight": "node scripts/preflight-package.mjs",
    "//check-conversation": "Deliberately NOT part of `check` above. Every other check gate scans this checkout \u2014 offline, hermetic, exactly reproducible on a fork's CI run with no credentials. This one scans GitHub's live API instead: it needs network access and an authenticated `gh`, neither of which a fork's CI or an offline run has, so wiring it into `check` would make the whole chain fail (or silently no-op) for every contributor who isn't the repository owner. Run it separately, where those preconditions are actually met.",
    "check:conversation": "node scripts/check-conversation-safety.mjs --all",
    "//check-package-visibility": "Also deliberately NOT part of `check` above, for the same reason as check:conversation directly above: this calls the live GitHub Packages API and needs a read:packages token (GH_PACKAGES_TOKEN) to do it at all, which no fork's CI and no ordinary local `npm run check` should be required to hold. See the script's own header for what it checks and why. Wired into CI as a post-publish job in publish.yml and a scheduled run in package-visibility.yml, both of which already have the credential for a narrower reason.",
    "check:package-visibility": "node scripts/check-package-visibility.mjs",
    "//check-registry-parity": "Also deliberately NOT part of `check` above, same reason as check:package-visibility directly above: this calls the live GitHub Packages API and needs GH_PACKAGES_TOKEN. FAILS when a non-private packages/*/package.json's version has no registry counterpart -- the safety net for issue #416 (an evicted publish.yml run reports `cancelled`, not `failure`, so nothing else goes red). See the script's own header. Wired into CI as .github/workflows/registry-parity.yml's scheduled run, which already has the credential for a narrower reason; deliberately not a required status context -- that promotion is left to a human.",
    "check:registry-parity": "node scripts/check-registry-parity.mjs",
    "//check-conversation-draft": "The prevention mode: scan text BEFORE it is posted to an issue or PR, while withholding it is still free. Pass --file <path>, or pipe the draft text in: `echo \"...\" | npm run check:conversation:draft --`.",
    "check:conversation:draft": "node scripts/check-conversation-safety.mjs"
  },
  "devDependencies": {
    "typescript": "~6.0.0"
  }
}
