/**
 * @vespeneventures/policy — DEPRECATED. Migrate to
 * `@vespeneventures/controller/policy`, which owns this source now (see
 * issue #282). Every export below forwards there so existing imports keep
 * working during the migration window. Issue #288 removes this
 * compatibility package.
 */
export * from "@vespeneventures/controller/policy";
