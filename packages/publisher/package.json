{
  "name": "@clossys/publisher",
  "version": "0.6.0",
  "private": false,
  "license": "MIT",
  "type": "module",
  "sideEffects": false,
  "description": "The publisher role: did we put it out to an audience, and can we prove what shipped? Surface composition, media registries, and channel renderers for web, email, print, images, and slides, plus a product-neutral structured-document contract, under `./core`, `./media`, `./web`, `./document`, `./email`, `./print`, `./image`, and `./slides`; an append-only, content-addressed record of what was published, to which channel, when, and citing which facts, plus a drift checker, under `./record`. One package, one version: every time the publisher runs, the record runs, and the record still does not import the composer.",
  "keywords": [
    "publisher",
    "composition",
    "render",
    "web",
    "email",
    "print",
    "image",
    "slides",
    "ledger",
    "append-only",
    "attribution",
    "drift",
    "content-addressed",
    "typescript"
  ],
  "author": "Clossys",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/clossys/foundry.git",
    "directory": "packages/publisher"
  },
  "bugs": {
    "url": "https://github.com/clossys/foundry/issues"
  },
  "homepage": "https://github.com/clossys/foundry/tree/main/packages/publisher#readme",
  "engines": {
    "node": ">=20"
  },
  "exports": {
    "./core": {
      "types": "./dist/core/index.d.ts",
      "import": "./dist/core/index.js"
    },
    "./assessment": {
      "types": "./dist/verified-publication-rate.d.ts",
      "import": "./dist/verified-publication-rate.js"
    },
    "./media": {
      "types": "./dist/media/index.d.ts",
      "import": "./dist/media/index.js"
    },
    "./web": {
      "types": "./dist/web/index.d.ts",
      "react-server": "./dist/web/server.js",
      "import": "./dist/web/index.js"
    },
    "./document": {
      "types": "./dist/document/index.d.ts",
      "import": "./dist/document/index.js"
    },
    "./email": {
      "types": "./dist/email/index.d.ts",
      "import": "./dist/email/index.js"
    },
    "./print": {
      "types": "./dist/print/index.d.ts",
      "import": "./dist/print/index.js"
    },
    "./image": {
      "types": "./dist/image/index.d.ts",
      "import": "./dist/image/index.js"
    },
    "./slides": {
      "types": "./dist/slides/index.d.ts",
      "import": "./dist/slides/index.js"
    },
    "./record": {
      "types": "./dist/record/index.d.ts",
      "import": "./dist/record/index.js"
    },
    "./pack": {
      "types": "./dist/pack/index.d.ts",
      "import": "./dist/pack/index.js"
    },
    "./surfaces": {
      "types": "./dist/surfaces/index.d.ts",
      "import": "./dist/surfaces/index.js"
    },
    "./materials": {
      "types": "./dist/materials/index.d.ts",
      "import": "./dist/materials/index.js"
    },
    "./templates": {
      "types": "./dist/templates/index.d.ts",
      "import": "./dist/templates/index.js"
    }
  },
  "imports": {
    "#publisher-web-views": {
      "types": "./dist/web/views/index.d.ts",
      "react-server": "./dist/web/views/server.js",
      "default": "./dist/web/views/index.js"
    }
  },
  "bin": {
    "publisher-media-check": "dist/media/cli.js",
    "publisher-record-check": "dist/record/cli.js",
    "publisher-rate-check": "dist/verified-publication-rate-cli.js",
    "publisher-web-route-check": "dist/web/checkWebRoutesCli.js",
    "publisher-preview": "dist/preview/cli.js"
  },
  "foundry": {
    "assessment": {
      "bin": "publisher-rate-check",
      "invocation": "single-json-input"
    },
    "outputs": [
      "clossys/publisher/pack.json",
      "clossys/publisher/surfaces/"
    ],
    "fit": "fit-signals.json",
    "feeds": [
      {
        "artifact": "surface-documents",
        "path": "clossys/publisher/surfaces/"
      }
    ],
    "capabilities": [
      {
        "id": "v0-launch-pack-planning-and-inventory",
        "subQuestion": "What does this product owe its v0 Launch pack, and what already exists versus is still needed?",
        "worldClass": "Every new product gets one declared inventory (clossys/publisher/pack.json, #1204) of what the v0 Launch pack owes it and what state each item is actually in — status, condition, version, source pins — never assembled ad hoc per product. Today: `src/pack/` ships the manifest contract (#1204) — `validatePackManifest` (schema, needs-graph, and lifecycle-vocabulary validation), `computePackReadiness`/`planPackOrder`/`sealableItemIds` (readiness and sealing order derived from the needs graph), and `detectExistingPackItems`/`foundPackItem` (adopt-don't-override detection with sha256 fingerprints) — as library functions over `PackManifest`; no CLI yet assembles or checks a real product's `clossys/publisher/pack.json` from them.",
        "inputs": [
          {
            "producerRole": "@clossys/strategist",
            "artifact": "strategy-brief"
          },
          {
            "producerRole": "@clossys/designer",
            "artifact": "brand-kit-assembly"
          },
          {
            "producerRole": "@clossys/writer",
            "artifact": "messaging-kit"
          }
        ],
        "outputs": [
          "clossys/publisher/pack.json"
        ],
        "proofCase": "media-satisfied",
        "maturity": "partial",
        "v0": true,
        "businessLifecycleStage": "launch"
      },
      {
        "id": "surface-documents",
        "subQuestion": "Is every audience-facing surface declared once as a validated document, with its template and locations resolved?",
        "worldClass": "Every published surface is one validated SurfaceDocument resolving to a registered template and a real channel location — never a page composed ad hoc outside the document. Today: `validateSurfaceDocument` and `resolveSurfaceDocument` (`src/core/`) validate and resolve one document as library functions, and the shipped site template (`templates/site/app/*/page.tsx`) reads `clossys/publisher/surfaces/*.json` at build time, but no bin yet validates a consumer's own surface documents, so no qualification case exercises this capability directly (#1272).",
        "inputs": [
          {
            "producerRole": "@clossys/writer",
            "artifact": "copy-registry"
          },
          {
            "producerRole": "@clossys/designer",
            "artifact": "components-and-blocks"
          }
        ],
        "outputs": [
          "clossys/publisher/surfaces/"
        ],
        "proofCase": "media-satisfied",
        "maturity": "partial",
        "v0": true,
        "businessLifecycleStage": "launch"
      },
      {
        "id": "channel-rendering",
        "subQuestion": "Does every declared surface render correctly to its channel — web, email, image, print, document, or slides?",
        "worldClass": "A surface document renders byte-identically to its golden fixture on every channel it declares, checked, never eyeballed per release.",
        "inputs": [
          {
            "producerRole": "@clossys/designer",
            "artifact": "token-contract-and-brand-overlay-binding"
          }
        ],
        "outputs": [
          "clossys/publisher/.generated/render-report.json"
        ],
        "proofCase": "preview-rendered",
        "maturity": "built",
        "v0": true,
        "businessLifecycleStage": "launch"
      },
      {
        "id": "templates-and-channel-specs",
        "subQuestion": "Is every channel's template and spec declared once and reused, never improvised per page?",
        "worldClass": "A channel template is data — a declared block sequence and spec — never an arbitrary per-page build function, per #1207's full set (overview, deck, email kit, social profile, video-call backgrounds). Today: `src/templates/` ships `overviewTemplate.ts`, `deckTemplate.ts`, `emailSignature.ts` (`buildEmailSignatureHtml`/`buildEmailSignatureText`), and `channelSpecs.ts` (`SOCIAL_CHANNEL_SPECS`, `OG_SHARE_CARD_SPEC`, `VIDEO_CALL_BACKGROUND_SPECS`, `staleChannelSpecEntries`) alongside the earlier web page templates (#1207); nothing yet assembles them into the declared `clossys/publisher/templates.json` output or checks them from a bin.",
        "inputs": [],
        "outputs": [
          "clossys/publisher/templates.json"
        ],
        "proofCase": "media-satisfied",
        "maturity": "partial",
        "v0": true,
        "businessLifecycleStage": "build"
      },
      {
        "id": "materials-site",
        "subQuestion": "Is there one internal, locally opened materials index a client can browse every version of?",
        "worldClass": "One assembled, current internal index (#1206) carries every version of the company overview (short/medium/long) and pitch deck (HTML slides with audience variants), each with a print stylesheet, opened locally or handed to a client — never a scatter of one-off files assembled by hand. Not a publicly deployed site: that is #1208's separate apps/site template. Today: `src/materials/` ships `renderMaterialsIndexHtml`, `renderPitchDeckHtml`, `selectAudienceVariant`, and `materialsPrintStylesheet` (#1206), but `renderMaterialsIndexHtml` takes its entries as a plain argument — nothing yet reads a product's own `clossys/publisher/pack.json` to derive them, or writes the declared `clossys/publisher/materials/index.json` output from a bin.",
        "inputs": [
          {
            "producerRole": "@clossys/writer",
            "artifact": "messaging-kit"
          },
          {
            "producerRole": "@clossys/designer",
            "artifact": "brand-kit-assembly"
          }
        ],
        "outputs": [
          "clossys/publisher/materials/index.json"
        ],
        "proofCase": "preview-rendered",
        "maturity": "partial",
        "v0": true,
        "businessLifecycleStage": "launch"
      },
      {
        "id": "channel-kits",
        "subQuestion": "Is there one governed kit — assets, copy slots, specs — per channel a client can hand to that channel's own tool?",
        "worldClass": "Every channel (email signatures, social profiles, video-call backgrounds, per #1207) has one governed kit of checked assets and copy slots at the exact sizes and roles that channel requires — never assembled by hand per send.",
        "inputs": [
          {
            "producerRole": "@clossys/writer",
            "artifact": "messaging-kit"
          },
          {
            "producerRole": "@clossys/designer",
            "artifact": "logo-and-identity-files"
          }
        ],
        "outputs": [
          "clossys/publisher/channel-kits.json"
        ],
        "proofCase": "media-satisfied",
        "maturity": "partial",
        "v0": true,
        "businessLifecycleStage": "launch"
      },
      {
        "id": "asset-roster-and-coverage",
        "subQuestion": "Does every asset a document references resolve to a registered, checked entry at its required role and size?",
        "worldClass": "Every image/video asset a document binds resolves to a registered roster entry (favicon, OG, Twitter, email-safe, app icons…) at its required pixel size with alt text — checked both directions, never a broken or orphaned reference.",
        "inputs": [],
        "outputs": [
          "clossys/publisher/asset-roster.json"
        ],
        "proofCase": "media-satisfied",
        "maturity": "built",
        "v0": true,
        "businessLifecycleStage": "build"
      },
      {
        "id": "route-and-visibility-governance",
        "subQuestion": "Does every publishing web route resolve to a registered template, with visibility declared as internal or public rather than inferred, and internal-only routes refused from serving?",
        "worldClass": "Every route composes only through a registered SurfaceDocument template -- never Designer blocks assembled directly in a route file -- and declares visibility as `internal` or `public` (#1204; supersedes the earlier public/unlisted/access-protected model #1206 retired); a route serving an internal-only item is refused, never silently served. Today: `PACK_VISIBILITIES` (`internal`/`public`) and `checkMaterialsVisibility` declare and check visibility at the materials level (#1206), but `checkWebRoutes`/`publisher-web-route-check` still checks only the route-to-template mapping -- no route-level visibility declaration or internal-route refusal runs yet.",
        "inputs": [],
        "outputs": [
          "clossys/publisher/routes.json"
        ],
        "proofCase": "web-routes-clean",
        "maturity": "partial",
        "v0": true,
        "businessLifecycleStage": "launch"
      },
      {
        "id": "site-template",
        "subQuestion": "Does a product repository get a working marketing site wired to its team's own records from the first commit?",
        "worldClass": "An apps/site template (#1208) renders Publisher's web views and Designer's theme, reading copy, tokens, and assets from clossys/ at build time and never copying them — routes for landing, about, contact, privacy/terms, 404, metadata, sitemap, robots, all included. Today: `templates/site/` ships all of the above (#1208), plus `web-route-manifest.json` for `publisher-web-route-check`, but is template content copied by Launcher (#1215) into a consumer repository, not part of this package's own npm workspace — its own README says plainly it is \"not built, typechecked, or tested\" by this repository's `npm run build`/`npm run typecheck`/`npm test`, and Launcher's own apply step (#1215) is still a follow-up.",
        "inputs": [
          {
            "producerRole": "@clossys/publisher",
            "artifact": "route-and-visibility-governance"
          }
        ],
        "outputs": [
          "clossys/publisher/site.json"
        ],
        "proofCase": "media-satisfied",
        "maturity": "partial",
        "v0": true,
        "businessLifecycleStage": "launch"
      },
      {
        "id": "sealing-and-the-publication-record",
        "subQuestion": "Once published, is the exact shipped result sealed into an immutable, append-only record, with every cited fact still current?",
        "worldClass": "Every publication event is appended once to an immutable record — never overwritten, never silently lost — and every fact cited in what was sealed is re-checked against the current source value at seal time, with drift reported, never assumed absent. A surface seals only once Customer has recorded a keep.",
        "inputs": [
          {
            "producerRole": "@clossys/customer",
            "artifact": "keep-verdict"
          }
        ],
        "outputs": [
          "clossys/publisher/record.json"
        ],
        "proofCase": "record-append-only-clean",
        "maturity": "built",
        "v0": true,
        "businessLifecycleStage": "deliver"
      },
      {
        "id": "live-parity",
        "subQuestion": "Does the production URL actually serve what was sealed — head, OG, metadata, and the materials index — independent of the hosting provider's own success signal?",
        "worldClass": "`verify` (#1209) proves the live site actually serves what was sealed -- head, OG, and metadata for public surfaces -- independently re-reading the live URL, never trusting the deploy provider's own \"success\" status. Scoped to public surfaces only, per #1206 (materials visibility is declared per item; an internal item is never expected to be live-servable at all). Today: the reconciliation-loop contracts (an independent witness compared against a ledger) exist; nothing yet re-reads a live production URL.",
        "inputs": [
          {
            "producerRole": "@clossys/publisher",
            "artifact": "sealing-and-the-publication-record"
          }
        ],
        "outputs": [
          "clossys/publisher/.generated/live-parity-report.json"
        ],
        "proofCase": "media-satisfied",
        "maturity": "partial",
        "v0": false,
        "businessLifecycleStage": "operate"
      }
    ]
  },
  "files": [
    "dist",
    "src",
    "!src/**/*.test.ts",
    "!src/**/*.test.tsx",
    "CHANGELOG.md",
    "README.md",
    "LICENSE",
    "skill",
    "templates",
    "fit-signals.json"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "prepublishOnly": "node ../../scripts/check-name-collision.mjs . && npm run build",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@clossys/writer": "^0.3.0",
    "@clossys/designer": "^0.5.0",
    "@clossys/controller": "~0.9.0"
  },
  "peerDependencies": {
    "@internationalized/date": "^3.12.2",
    "react": ">=18",
    "react-aria-components": "^1.19.0",
    "react-dom": ">=18",
    "tailwind-merge": "^3.7.0",
    "tailwindcss": "^4.0.0"
  },
  "peerDependenciesMeta": {
    "@internationalized/date": {
      "optional": true
    },
    "react": {
      "optional": true
    },
    "react-aria-components": {
      "optional": true
    },
    "react-dom": {
      "optional": true
    },
    "tailwind-merge": {
      "optional": true
    },
    "tailwindcss": {
      "optional": true
    }
  },
  "devDependencies": {
    "@internationalized/date": "^3.12.3",
    "@testing-library/jest-dom": "^7.0.1",
    "@testing-library/react": "^16.3.3",
    "@testing-library/user-event": "^14.6.7",
    "@types/node": "^26.5.1",
    "@types/react": "^19.3.0",
    "@types/react-dom": "^19.3.0",
    "react": "^19.3.0",
    "react-aria-components": "^1.21.1",
    "react-dom": "^19.3.0",
    "jsdom": "^26.1.0",
    "tailwind-merge": "^3.7.0",
    "tailwindcss": "^4.3.3",
    "typescript": "~6.0.0",
    "vitest": "^5.0.1"
  },
  "publishConfig": {
    "registry": "https://registry.npmjs.org",
    "access": "public"
  }
}
