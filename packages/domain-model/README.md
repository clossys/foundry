# @vespeneventures/domain-model

This temporary compatibility package re-exports the complete public API from
the canonical package in [`../domain`](../domain). New consumers must migrate
their manifest and imports to that package directly.

No product values, storage, actions, authorization, provenance, lifecycle, or
runtime behavior is added by this package.
