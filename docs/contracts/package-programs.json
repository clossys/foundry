{
  "schemaVersion": 1,
  "$comment": "Where every package stands on the seven-state ladder in docs/LIFECYCLE.md, and what is missing below the state it claims. Graded by scripts/check-package-programs.mjs, wired as `npm run check:package-programs`. The one rule: a state is derived from evidence, never declared. A shortfall below a declared state must be acknowledged here with a reason and an issue -- an unacknowledged one fails the gate, and an acknowledgement that outlives its reason fails it too. The `gaps` list is a countdown, not a baseline: it is meant to shrink, and the gate refuses to let it go stale.",
  "programs": {
    "operation": {
      "letter": "A",
      "question": "Does the workspace do what it says it does?",
      "decision": "docs/DECISIONS.md#9-recutting-the-workspace-surface-into-six-job-shaped-packages",
      "packages": [
        "@vespeneventures/controller",
        "@vespeneventures/inspector",
        "@vespeneventures/builder",
        "@vespeneventures/locksmith",
        "@vespeneventures/integrator",
        "@vespeneventures/observer"
      ],
      "donors": []
    },
    "expression": {
      "letter": "B",
      "question": "Is what we put out true, well said, well made, and provable?",
      "decision": "docs/DECISIONS.md#10-recutting-the-expression-surface-into-role-shaped-packages",
      "packages": [
        "@vespeneventures/strategist",
        "@vespeneventures/writer",
        "@vespeneventures/designer",
        "@vespeneventures/publisher"
      ],
      "donors": [
        "@vespeneventures/strategy",
        "@vespeneventures/copy",
        "@vespeneventures/ui",
        "@vespeneventures/surface",
        "@vespeneventures/ledger"
      ]
    },
    "interaction": {
      "letter": "C",
      "question": "Is everything we do with one identified person accounted for?",
      "decision": "https://github.com/vespeneventures/foundry/issues/458",
      "packages": [
        "@vespeneventures/bouncer",
        "@vespeneventures/butler",
        "@vespeneventures/giver",
        "@vespeneventures/keeper"
      ],
      "donors": [
        "@vespeneventures/auth",
        "@vespeneventures/consent",
        "@vespeneventures/comms"
      ]
    },
    "foundation": {
      "letter": "-",
      "tier": "primitive",
      "question": "Not a programme. Packages that predate the three recuts and belong to no role.",
      "decision": "docs/DECISIONS.md#11-a-gate-behind-a-bin-or-a-declared-primitive",
      "$comment": "The primitive tier, per decision 11: no addressee, therefore no role, therefore no question only it answers, therefore nothing for a gate to judge. Membership is a declaration and never a fallback -- a member must declare `shipsNoGate` with `permanent: true` and a reason, and exposing a `bin` from here is itself a finding. `packages/domain/README.md` is what the tier's one member does; this decision is why it owes no gate for doing it.",
      "packages": [
        "@vespeneventures/domain"
      ],
      "donors": []
    }
  },
  "packages": [
    {
      "name": "@vespeneventures/controller",
      "state": "published",
      "stagedBy": {
        "run": "LOCAL, not a CI run — after `npm ci && npm run build`, `node packages/controller/dist/cli.js docs/contracts/package-lifecycle.json . --scope @vespeneventures` printed `Package governance: PASS` and exited 0. The injected red ran the same dist CLI with `packages/locksmith` as root; it printed `Package governance: FAIL`, one packages-dir-missing foundation finding, 16 catalog-package-missing lifecycle findings, and exited 1. 2026-08-22.",
        "defectOrigin": "injected",
        "defect": "A consumer pointing the governance CLI at a package directory instead of its repository root is a real catalog defect. The built controller gate discovered no packages directory and named every lifecycle package the incomplete catalog omitted; the gate itself produced exit 1.",
        "control": "The same compiled CLI, lifecycle contract, and scope against this repository root stayed GREEN in the same session: 18 packages, zero foundation findings, zero lifecycle findings, and exit 0."
      }
    },
    {
      "name": "@vespeneventures/inspector",
      "state": "published",
      "stagedBy": {
        "run": "LOCAL, not a CI run — after `npm ci && npm run build`, run `node packages/inspector/dist/bin.js --inputs <red-input> --checks secret-scan`; the injected observation printed `secret-detected (src/a.ts): example-scanner rule generic-api-key` and `Overall: VIOLATED (exit 1)`. With the otherwise identical control containing zero hits, the same dist CLI printed `secret-scan | satisfied | 5 evaluated` and `Overall: SATISFIED (exit 0)`. 2026-08-22.",
        "defectOrigin": "injected",
        "defect": "A scanner reporting a credential-shaped hit at a source path is the real secret-scan violation this gate consumes. The compiled inspector entry point rendered its own violated report and exit 1, with no mocked return or forced process status.",
        "control": "The same scanner observation shape with five evaluated units and no hits stayed GREEN in the same local session, proving the dist CLI distinguishes a reported hit from a clean scan."
      }
    },
    {
      "name": "@vespeneventures/builder",
      "state": "published",
      "stagedBy": {
        "run": "LOCAL, not a CI run — after `npm ci && npm run build`, run `node packages/builder/dist/ci/bin.js deployment-health --inputs <red-input>` with one healthy and one unhealthy surface. It printed `Overall: UNHEALTHY (exit 1)` with `healthy 1, degraded 0, unhealthy 1, unknown 0`. The control with the healthy surface only printed `Overall: HEALTHY (exit 0)`. 2026-08-22.",
        "defectOrigin": "injected",
        "defect": "An unhealthy deployment observation is a real input a consuming plane's deployment-health gate can receive. Builder's compiled dist CLI judged it unhealthy and supplied its normal exit 1 rather than a mock or forced exit status.",
        "control": "The otherwise same one-surface control with status healthy stayed GREEN in the same local session: builder printed HEALTHY, one healthy observation, and exit 0."
      }
    },
    {
      "name": "@vespeneventures/locksmith",
      "state": "published",
      "stagedBy": {
        "run": "LOCAL, not a CI run — reproduce after `npm ci && npm run build`: run `node packages/locksmith/dist/infisical/cli.js catalog --catalog governance/locksmith-catalog.json` (printed the two value-free registry credential entries and exited 0). For the injected red, run the same dist CLI against a version-1 catalog containing GH_PACKAGES_TOKEN twice; it printed `Secret catalog must be value-free version 1 metadata with unique keys.` and exited 2. 2026-08-22.",
        "defectOrigin": "injected",
        "defect": "A duplicate credential name is a real value-free catalogue defect a consuming plane can produce. The built locksmith parser, not a mocked return or forced exit code, rejected the duplicate-key catalogue and printed its own uniqueness failure.",
        "control": "The repository-owned governance/locksmith-catalog.json stayed GREEN in the same local staging session: the built dist CLI printed its two declared, value-free workflow credential classes and exited 0. The control differs only by having unique keys."
      }
    },
    {
      "name": "@vespeneventures/integrator",
      "state": "published",
      "stagedBy": {
        "run": "LOCAL, not a CI run — reproduce after `npm ci && npm run build`: run `node packages/integrator/dist/cli.js package.json governance/foundry-supersession-map.json --block` (printed `Evaluated 2 declared supersessions. No conflicting pairs installed.` and exited 0). For the injected red, run the same dist CLI with a manifest whose dependencies contain @vespeneventures/auth and @vespeneventures/bouncer; it printed `1 conflicting pair installed together` and exited 1. 2026-08-22.",
        "defectOrigin": "injected",
        "defect": "A manifest pinning both the retained auth donor and its published bouncer replacement is the real duplicate-install condition this supersession gate judges. The built CLI found the pair in dependencies and produced its normal blocking exit 1, not a mocked result or forced exit code.",
        "control": "The repository's root package.json stayed GREEN in the same local staging session against governance/foundry-supersession-map.json: both declared relationships were evaluated and no conflicting pair was installed, so the built CLI exited 0 in --block mode."
      }
    },
    {
      "name": "@vespeneventures/observer",
      "state": "published",
      "stagedBy": {
        "run": "LOCAL, not a CI run — after `npm ci && npm run build`, run `node packages/observer/dist/bin.js --input <red-input>` with observer installed in repo-a and the same package declared absent there. It printed the contradiction, `Overall: VIOLATED (exit 1)`. The control removed the declared absence and the same dist CLI printed `Overall: SATISFIED (exit 0)`. 2026-08-22.",
        "defectOrigin": "injected",
        "defect": "An installed package simultaneously declared absent is the real coverage contradiction observer exists to surface. Its compiled CLI found the contradiction and emitted its normal violated exit 1, not a mocked verdict or forced process code.",
        "control": "The otherwise identical installed inventory without a declared absence stayed GREEN in the same local session: observer classified the package installed with no contradiction and exited 0."
      }
    },
    {
      "name": "@vespeneventures/strategist",
      "state": "published",
      "gaps": [
        {
          "state": "staged",
          "reason": "zero dist-path invocation sites in this repository: this package has never been executed against a real tree by its own author",
          "issue": 466
        }
      ]
    },
    {
      "name": "@vespeneventures/writer",
      "state": "published",
      "gaps": [
        {
          "state": "staged",
          "reason": "zero dist-path invocation sites in this repository: this package has never been executed against a real tree by its own author",
          "issue": 466
        }
      ]
    },
    {
      "name": "@vespeneventures/designer",
      "state": "published",
      "stagedBy": {
        "run": "LOCAL, not a CI run — no Actions URL exists for it. Reproduce: build designer, copy packages/designer/styles/tokens.css, set --color-ink-primary to oklch(0.9702 0 0) (identical to --color-surface-base), then `node packages/designer/dist/tokens/contrast-cli.js <that file>`. 2026-08-22.",
        "defectOrigin": "injected",
        "defect": "--color-ink-primary set equal to --color-surface-base gives a 1:1 contrast ratio against the page ground. designer-contrast-check exited 1 with three light-theme findings, reached through the gate’s own contrast computation over CONTRAST_PAIRS — not a mock, a stub, or a forced exit code.",
        "control": "The dark theme stayed GREEN in the same run: 0 findings, while light produced 3. Only the light-theme value was altered, so the clean dark result shows the gate discriminates between a violating and a conforming palette rather than failing on any input it is handed."
      }
    },
    {
      "name": "@vespeneventures/publisher",
      "state": "published",
      "gaps": [
        {
          "state": "staged",
          "reason": "zero dist-path invocation sites in this repository: this package has never been executed against a real tree by its own author",
          "issue": 466
        }
      ]
    },
    {
      "name": "@vespeneventures/strategy",
      "state": "published"
    },
    {
      "name": "@vespeneventures/copy",
      "state": "published"
    },
    {
      "name": "@vespeneventures/ui",
      "state": "published"
    },
    {
      "name": "@vespeneventures/surface",
      "state": "published"
    },
    {
      "name": "@vespeneventures/ledger",
      "state": "published"
    },
    {
      "name": "@vespeneventures/auth",
      "state": "published",
      "shipsNoGate": {
        "issue": 464,
        "reason": "Deprecated donor to the published `bouncer` role -- who you are, what you can do, and how that changes. The role now owns the gate; this retained package has none and remains installable only while consumers migrate under issue #464."
      },
      "gaps": [
        {
          "state": "staged",
          "reason": "zero dist-path invocation sites in this repository: this package has never been executed against a real tree by its own author",
          "issue": 466
        }
      ]
    },
    {
      "name": "@vespeneventures/consent",
      "state": "published",
      "shipsNoGate": {
        "issue": 464,
        "reason": "Deprecated donor to the published `butler` and `giver` roles. The roles now own the gate and enforcement split; this retained package has none and remains installable only while consumers migrate under issue #464."
      },
      "gaps": [
        {
          "state": "staged",
          "reason": "zero dist-path invocation sites in this repository: this package has never been executed against a real tree by its own author",
          "issue": 466
        }
      ]
    },
    {
      "name": "@vespeneventures/comms",
      "state": "published",
      "shipsNoGate": {
        "issue": 464,
        "reason": "Program C donor whose `./inbound` surface overlaps the published `butler` role. The package still has no `bin`; issue #464 tracks removal of that overlap, while the remaining transport surface awaits a separate role decision rather than being declared a permanent primitive."
      },
      "gaps": [
        {
          "state": "staged",
          "reason": "zero dist-path invocation sites in this repository: this package has never been executed against a real tree by its own author",
          "issue": 466
        }
      ]
    },
    {
      "name": "@vespeneventures/domain",
      "state": "published",
      "shipsNoGate": {
        "permanent": true,
        "reason": "The primitive tier's first declared member. It defines identifiers, typed fields, closed vocabularies and relations and ships no values, storage, authorization, provenance or lifecycle of its own, so the only thing a gate of its own could judge is the consumer's model -- which is the consumer's judgment to make, not this package's. Its checkers are library functions a consumer's own gate calls, not a command this package installs."
      },
      "gaps": [
        {
          "state": "staged",
          "reason": "zero dist-path invocation sites in this repository: this package has never been executed against a real tree by its own author",
          "issue": 466
        }
      ]
    },
    {
      "name": "@vespeneventures/bouncer",
      "state": "published",
      "gaps": [
        {
          "state": "staged",
          "reason": "This package has never been executed against a real tree by its own author",
          "issue": 507
        }
      ]
    },
    {
      "name": "@vespeneventures/butler",
      "state": "published",
      "gaps": [
        {
          "state": "staged",
          "reason": "This package has never been executed against a real tree by its own author",
          "issue": 507
        }
      ]
    },
    {
      "name": "@vespeneventures/giver",
      "state": "published",
      "gaps": [
        {
          "state": "staged",
          "reason": "This package has never been executed against a real tree by its own author",
          "issue": 507
        }
      ]
    },
    {
      "name": "@vespeneventures/keeper",
      "state": "published",
      "gaps": [
        {
          "state": "staged",
          "reason": "This package has never been executed against a real tree by its own author",
          "issue": 507
        }
      ]
    }
  ]
}
