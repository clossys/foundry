# Installing @clossys packages needs no token: they are published to public
# npmjs, which is already npm's default registry, so this line is redundant
# rather than required. It is kept only as an explicit statement of the route.
# Never commit a real .npmrc; it is gitignored and the safety gate refuses it
# by name regardless.

@clossys:registry=https://registry.npmjs.org
