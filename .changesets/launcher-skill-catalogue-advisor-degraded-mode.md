---
launcher: minor
---

Launcher's packed skill catalogue carries Advisor's updated `SKILL.md`, which states an explicit degraded mode outside the hub -- read-only status and questions in every case, hiring/plan-change/approval decisions refused outside the hub, and the exact `npx --package=@clossys/advisor@<hub version> <bin>` invocation for running a bin without installing the package (#1507).
