---
launcher: minor
---

A change set can refuse a path as `deleted`, `release-age-surface-conflict` or `release-age-surface-unparseable`, and a file mode is `100644`, or `120000` for a discovery link; `100755` is refused (#1178).
