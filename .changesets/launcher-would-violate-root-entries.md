---
launcher: minor
---

New `wouldViolateRootEntries()` judges whether the root names some paths introduce would fail a Controller repository profile's root vocabulary, naming the undeclared and the prohibited names, and is indeterminate for a profile Controller could not read as one; new `isRootEntryName()` is Controller's rule for one direct-child name (#1178).
