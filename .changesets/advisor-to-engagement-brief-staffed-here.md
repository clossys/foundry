---
advisor: minor
---

`toEngagementBrief()` takes an optional `staffedHere` and writes it into the brief in the order given; it throws, naming only the position, when the list is empty or an entry is not one of the composed roles or repeats (#1178).
