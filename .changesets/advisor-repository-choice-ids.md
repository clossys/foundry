---
advisor: minor
---

Every repository id the card offers satisfies the repository inventory contract's id rule (`docs/contracts/repository-inventory.json`, now packed beside the plan and brief contracts) and is `owner/name`, so `@clossys/launcher` accepts it (#1179).
