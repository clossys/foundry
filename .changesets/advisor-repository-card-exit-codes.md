---
advisor: minor
---

`advisor-repository-card` exits `0` for a card or an accepted choice, `1` when the repository list given is empty or the choice is refused, and `2` for unreadable or invalid input (#1179).
