---
launcher: minor
---

New `planApplyBundle()` computes one change set per staffed repository and a report-mode bundle from a plan, the hub brief and observations of each repository's default branch, reading no file, network, process or clock; it carries every package act the plan names for a repository as an item or a deferral, refuses any file it would write whole, and any `package.json` key it would change, that the default branch already has, and records no repository state (#1178).
