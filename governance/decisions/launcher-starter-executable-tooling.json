{
  "schemaVersion": 1,
  "id": "launcher-starter-executable-tooling",
  "tier": "tier-1",
  "question": "Are @clossys/launcher and @clossys/starter roles under docs/contracts/role-loop-archetypes.json, or executable tooling -- and should the Stage B conformance gates (check-package-conformance.mjs, check-loop-matrix.mjs) keep silently skipping any packages/* manifest absent from the roles map?",
  "options": [
    "Exclude both from roles and classify them as executable tooling, as a declared, gate-reported state (not a silent skip): reuse docs/contracts/package-evidence.json's existing category: \"executable-tooling\" field rather than inventing a second list; report an excluded row for each on the two Stage B gates; make a package in neither the roles map nor executable-tooling (or in both) a hard finding in every mode, not just report mode. Keep both packages in Stage C with a reduced checklist.",
    "Add launcher and starter to role-loop-archetypes.json's roles map as ordinary roles, subject to the full Stage B checklist.",
    "Leave the gates as-is: any packages/* manifest missing from the roles map disappears from both gates with no finding at all."
  ],
  "recommendation": "Option 1, confidence high: the owner has already classified both packages this way in their own package.json descriptions, READMEs, and docs/contracts/first-wave-sequence.json's wave kind: \"executable-tooling\" entries and job text (\"without treating executable tooling as a role\" / \"without treating the scaffolder as a role\"); docs/contracts/scope-migration.md's own count is 19 roles across 21 packages, with starter and launcher as executable tooling; neither package has an owned metric under role-loop-archetypes.json's own qualificationVerdicts (every loop either appears to run is already owned by an existing role, yielding extend or compose, never create); and launcher's real output paths (clossys/README.md, etc.) would fail package-framework.json's outputs.pathRule (clossys/<role>/...) by construction if it were graded as a role.",
  "reviews": [
    {
      "role": "primary",
      "instance": "unrecorded-predates-schema",
      "provider": "anthropic",
      "model": "opus",
      "effort": "unrecorded-predates-schema",
      "verdict": "approve",
      "link": "https://github.com/clossys/foundry/issues/1187#issuecomment-5800189482"
    },
    {
      "role": "secondary",
      "instance": "unrecorded-predates-schema",
      "provider": "unrecorded-predates-schema",
      "model": "unrecorded-predates-schema",
      "effort": "unrecorded-predates-schema",
      "verdict": "changes",
      "link": "https://github.com/clossys/foundry/pull/1318#issuecomment-5800994982"
    }
  ],
  "status": "open",
  "decidedBy": null,
  "decision": null,
  "relaxesGateOrPolicy": false,
  "sunset": null,
  "expiry": "2026-10-23T00:00:00Z",
  "supersedes": [],
  "links": {
    "pullRequests": ["1318"],
    "issues": ["1187", "1015", "505", "511"],
    "paths": [
      "scripts/package-classification.mjs",
      "scripts/check-package-conformance.mjs",
      "scripts/check-loop-matrix.mjs"
    ]
  },
  "notes": "NOT DECIDED (#1187 review at 8e6d97ea, blocking finding 7 -- an earlier draft of this record wrongly marked it status: \"decided\"/decidedBy: \"consensus\" and said implementation was complete; corrected here). PR #1318 implements option 1 per the Opus recommendation at #1187 comment 5800189482 ('Decision 1'), which recommended the tier-1 consensus path as reversible and matching an existing owner classification -- that recommendation is recorded as the 'primary' review above, verdict approve. An independent review at commit e039cb7b (https://github.com/clossys/foundry/pull/1318#issuecomment-5800994982) returned BLOCKING, not approve: it verified the role-row and enforcement claims held, but found a real, reproduced gap -- a packages/*/package.json with a missing, empty, or non-string \"name\" still silently disappears from both gates with zero finding, exactly the defect this PR exists to close, in the one case (an unreadable manifest name) its own new classification logic does not cover. That is recorded as the 'secondary' review above, verdict changes (a tier-1 disagreement, per the decision-tier rule's 'if they disagree ... escalate'). As of this record's own writing, a further PR comment (https://github.com/clossys/foundry/pull/1318#issuecomment-5801069249, posted after the blocking review) claims the flagged gap is fixed at commit 5db390f8 and asks for a fresh independent review -- but no such re-review had posted yet, so #1318 remains open and this decision remains open too. `decidedBy` and `decision` are null because nothing has been decided; `expiry` is set to 30 days out (2026-10-23) as a working default for revisiting an open tier-1 item, not a claim about when #1318 will actually resolve. Once a genuine tier-1 consensus (or an owner ruling, if the reviewers still disagree) is reached, this record should be superseded by a new, decided record -- not edited in place."
}
