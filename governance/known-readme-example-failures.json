{
  "issue": "#908",
  "note": "check-readme-examples.mjs's one pre-existing, measured finding. @clossys/writer's README shows a GlossaryEntry literal missing the required `caseSensitive` field (issue #908's own reproduction). This is packed content — fixing it moves writer's package tree and needs its own version and qualification record (docs/PUBLISHING.md) — so it rides writer's next version bump rather than landing inside this repository-infrastructure change. This file is a RATCHET, not an exemption: the gate refuses to let the set grow, and an entry that no longer matches anything is itself a finding, so fixing the block and deleting its entry are the same commit. Every run prints the waived set. When this file is empty, delete it and close #908's writer follow-up.",
  "packages": {
    "writer": [
      {
        "block": 2,
        "code": "TS2741",
        "reason": "README.md's VoiceRecord example glossary entry omits `caseSensitive`, which GlossaryEntry requires — see issue #908."
      }
    ]
  }
}
