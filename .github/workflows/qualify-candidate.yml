name: Qualify candidate

# Produce and retain a pre-publication release-qualification record for one
# already-merged package version, on the exact pinned runtime
# scripts/lib/release-runtime.mjs requires (issue #948).
#
# WHY THIS WORKFLOW EXISTS
# ------------------------
# scripts/run-candidate-qualification.mjs refuses before it packs anything
# unless the runtime is EXACTLY Node v24.19.0, npm 11.17.0, and zlib
# 1.3.2.1-motley-3246f1b (scripts/lib/release-runtime.mjs). That pin is
# correct and must not be relaxed: a qualification record exists to prove
# exact artifact bytes, and bytes produced by a different toolchain prove
# different bytes. A developer machine running a different Node/npm/zlib
# tuple cannot produce a record at all, and every affected merge has instead
# accumulated an acknowledged, issue-referenced deferral file under
# governance/release-qualification-deferrals/ — a deferral acknowledges a
# merge, never a publication, so every one of those versions has stayed
# unpublishable.
#
# ci.yml's own required `build and test` job already proves this exact
# runtime is available for free in GitHub Actions, in its
# "Assert qualified-directory release runtime" step: it pins the identical
# actions/setup-node commit SHA and asserts the identical three values. This
# workflow reuses that shape rather than inventing a second one, and is the
# workflow #948's own close path names: "Produce the record on a machine
# running the pinned runtime — or in the workflow that already runs it."
#
# CREDENTIAL-FREE QUALIFICATION, ON PURPOSE
# ------------------------------------------
# scripts/lib/candidate-runner.mjs's sanitizedEnv() deliberately builds the
# qualification child's environment from a hardcoded literal rather than
# spreading process.env, so a candidate can never inherit a parent-shell
# credential, and assertCredentialFree() (called before qualification starts)
# refuses an ambient NODE_AUTH_TOKEN/NPM_TOKEN/GH_PACKAGES_TOKEN/
# GITHUB_TOKEN/GH_TOKEN outright. Every step up to and including "Run fixed
# candidate qualification credential-free" below therefore never sets any of
# those, this job never uses the npm-publish environment, and no step here
# adds a CA-trust variable (NODE_EXTRA_CA_CERTS, NODE_USE_SYSTEM_CA) either.
# AGENTS.md's "Cloud sessions" section explains the hazard that guards
# against: in a sandbox class whose outbound HTTPS is transparently
# intercepted and re-terminated by a policy-enforcing egress proxy, trusting
# that proxy's CA would mean qualification — a gate whose entire purpose is
# proving exact artifact bytes — trusting an interception path for the very
# bytes it qualifies. That is not a call this workflow makes unilaterally,
# so it does not forward a CA-trust variable, and instead runs on a
# GitHub-hosted runner talking to the real, credential-free public registry.
#
# THE WRITE TOKEN LIVES IN A DIFFERENT JOB FROM THE CANDIDATE
# ----------------------------------------------------------
# The rule this workflow is built to: no job that executes candidate code
# holds a write-scoped token. Qualification deliberately imports and runs
# the candidate package and its registry dependency closure, and nothing
# about sanitizedEnv(), assertCredentialFree() or --ignore-scripts stops
# code the qualification imports on purpose. So the work is split in two
# jobs, and the boundary between them is the job, not a step:
#
#   qualify           permissions: contents: read, no secrets, checkout with
#                     persist-credentials: false. Installs, builds, packs,
#                     qualifies and generates the record, then uploads that
#                     one record file as an artifact. Everything it produces
#                     is untrusted from here on.
#   open-pull-request needs: qualify. permissions: contents: write and
#                     pull-requests: write. A FRESH checkout of the dispatched
#                     commit (github.sha); never runs npm ci, npm install or
#                     any package script, and never executes anything from
#                     the artifact. It downloads the artifact outside the
#                     workspace and hands it to
#                     scripts/accept-qualification-handoff.mjs, run from that
#                     fresh checkout, which accepts exactly one bounded
#                     qualification-record.json, re-derives its destination
#                     path, and validates it with validateCandidateQualification
#                     against joins recomputed from the trusted tree — failing
#                     closed on anything else. The stale deferral file, if
#                     any, is removed here by the trusted
#                     remove-qualification-deferral.mjs; no file removal
#                     crosses the hand-off.
#
# An earlier revision ran all of this in ONE job with contents: write and
# pull-requests: write granted workflow-wide, and argued the token was safe
# because it appeared only in the push step's own `env:`. Step-scoped env is
# not a boundary: every step in a job shares one runner, one workspace and
# one $HOME, so code running during qualification can leave behind
# something a later step executes (a git hook, a modified script, a PATH
# shim), and on a GitHub-hosted runner a step with passwordless sudo can read
# the runner process's memory, which holds the job's GITHUB_TOKEN whatever
# any step's env says. A job boundary gives the second job a fresh VM and a
# token the first job never had.
#
# The worst a malicious candidate can still achieve is a pull request
# carrying a qualification record that passes every acceptance check above;
# that pull request still needs human review and this repository's required
# CI before it can merge. Acceptance re-derives every join the trusted tree
# determines; it cannot re-derive candidate.tarball's digests or the
# transcript's observations without re-running qualification, so those are
# only as trustworthy as the qualify job that produced them, and publish.yml
# independently re-verifies the exact tarball bytes against them before any
# upload. scripts/qualify-candidate-workflow.test.mjs pins this shape.
#
# Three operating constraints follow from the split:
# - Retry with "Re-run all jobs". "Re-run failed jobs" on open-pull-request
#   alone fails closed by design: the artifact name and the review reference
#   the record must carry both include github.run_attempt, and a re-run
#   attempt has no qualify job of its own.
# - The split relies on actions/download-artifact refusing zip entries that
#   would extract outside its download path (CVE-2024-42471, fixed in
#   v4.1.7); the pinned 37930b1c... is v7.0.0.
# - open-pull-request must never restore a cache: its actions/setup-node
#   step has no `cache:` input and must not gain one, because a cache entry
#   is a channel any earlier job on this ref could have written.
#
# WHAT THIS WORKFLOW DOES NOT DO
# -------------------------------
# It never publishes anything. Publication stays a separate, manually
# dispatched publish.yml run, gated behind its own npm-publish environment
# and its own preflight join against the retained record this workflow
# produces. It never touches main directly: it pushes a branch and opens a
# pull request for a human to review and merge, the same as any other
# change here.
on:
  workflow_dispatch:
    inputs:
      package:
        description: Package directory under packages/ to qualify
        required: true
        type: string
      version:
        description: >-
          Version to qualify. Leave empty to use the checked-out ref's
          current packages/<package>/package.json version — this workflow
          qualifies the checked-out commit's manifest as it stands, it never
          bumps a version itself, so an explicit value must already match
          that manifest or the run fails closed.
        required: false
        default: ""
        type: string

# Nothing workflow-wide: each job below declares exactly what it needs, so
# a job added later starts with no token scope rather than inheriting write.
permissions: {}

# Serial per package. Earlier revisions of this workflow needed this because
# two dispatches for the same package raced on governance/release-
# qualification-deferrals.json, a single shared file every dispatch read and
# rewrote -- since #1254 moved that store to one file per package@version
# (governance/release-qualification-deferrals/<package>@<version>.json),
# dispatches for two DIFFERENT versions of the same package no longer touch
# the same deferral file. The group stays per-package regardless, to bound
# how many claude/qualify-<package>-* pull requests can be open for review at
# once; a retry of the SAME version still must not race itself.
concurrency:
  group: qualify-candidate-${{ inputs.package }}
  cancel-in-progress: false

jobs:
  qualify:
    # Executes the candidate and its dependency closure: read-only, no
    # secrets. See the header's job-split section.
    permissions:
      contents: read
    runs-on: ubuntu-latest
    timeout-minutes: 40
    steps:
      - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4.4.0
        with:
          fetch-depth: 0
          persist-credentials: false

      # Validated FIRST, before any later step interpolates ${{ inputs.package }}
      # into a shell path or a `working-directory:` field — the same ordering
      # publish.yml uses for its own "Validate package input" step, and for
      # the same reason: a workflow_dispatch string input is untrusted until
      # shape-checked.
      - name: Validate package input
        env:
          PKG: ${{ inputs.package }}
        run: |
          if ! [[ "$PKG" =~ ^[a-zA-Z0-9_-]+$ ]]; then
            echo "::error title=Invalid package input::package must match ^[a-zA-Z0-9_-]+\$ (got: $PKG)"
            exit 1
          fi
          if [ ! -f "packages/$PKG/package.json" ]; then
            echo "::error title=Unknown package::packages/$PKG/package.json does not exist."
            exit 1
          fi

      - uses: actions/setup-node@49933ea5288caeca8642d1e84afbd3f7d6820020 # v4.4.0
        with:
          node-version: 24.19.0

      - name: Assert qualified-directory release runtime
        run: |
          test "$(node --version)" = 'v24.19.0'
          test "$(npm --version)" = '11.17.0'
          test "$(node -p 'process.versions.zlib')" = '1.3.2.1-motley-3246f1b'

      # This workflow qualifies the checked-out commit's own manifest — it
      # never bumps a version. An explicit `version` input is accepted only
      # as a same-value confirmation, so a stale or mistaken dispatch fails
      # closed instead of silently qualifying whatever main happens to carry.
      - name: Resolve target version
        id: candidate
        env:
          PKG: ${{ inputs.package }}
          REQUESTED_VERSION: ${{ inputs.version }}
        run: |
          set -euo pipefail
          manifest_version="$(node -p "require('./packages/' + process.env.PKG + '/package.json').version")"
          if [ -n "$REQUESTED_VERSION" ] && [ "$REQUESTED_VERSION" != "$manifest_version" ]; then
            echo "::error title=Version mismatch::Requested version $REQUESTED_VERSION does not match packages/$PKG/package.json's current version $manifest_version. This workflow qualifies the checked-out ref's manifest version only; dispatch it on the ref whose package.json already carries the requested version."
            exit 1
          fi
          echo "version=$manifest_version" >> "$GITHUB_OUTPUT"

      - name: Install repository dependencies without lifecycle scripts
        run: npm ci --ignore-scripts

      # Unscoped, matching publish.yml's own qualify job: a candidate package
      # can have a first-party dependency (builder and inspector resolve
      # through controller) whose own dist/ must exist first, and
      # generate-qualification-record.mjs's reproducibility check below
      # rebuilds the whole workspace from a clean dist/ anyway.
      - name: Build
        run: npm run build

      - name: Pack one pre-publication candidate
        working-directory: packages/${{ inputs.package }}
        env:
          PKG: ${{ inputs.package }}
        run: |
          set -euo pipefail
          destination="$RUNNER_TEMP/qualification/$PKG"
          mkdir -p "$destination"
          result="$(npm pack --ignore-scripts --json --pack-destination "$destination")"
          filename="$(node -e 'console.log(JSON.parse(process.argv[1])[0].filename)' "$result")"
          mv "$destination/$filename" "$destination/candidate.tgz"

      - name: Run fixed candidate qualification credential-free
        env:
          PKG: ${{ inputs.package }}
          TARBALL: ${{ runner.temp }}/qualification/${{ inputs.package }}/candidate.tgz
          TRANSCRIPT: ${{ runner.temp }}/qualification/${{ inputs.package }}/transcript.json
        run: node scripts/run-candidate-qualification.mjs --package "$PKG" --tarball "$TARBALL" --output "$TRANSCRIPT"

      # The step above can fail two ways that print nothing useful on their
      # own: a thrown Error (a stack trace, but no transcript on disk yet)
      # or a graceful transcript.ok===false (process.exitCode=1, no stack,
      # no console output at all -- the failure detail lives only in the
      # transcript file this step writes). Without this, a run like #948's
      # writer dispatch surfaces as a bare "exit code 1" with nothing to
      # act on, which is exactly the failure mode candidateNotInstalledMessage()
      # exists to prevent one layer down.
      - name: Dump qualification transcript on failure
        if: failure()
        env:
          TRANSCRIPT: ${{ runner.temp }}/qualification/${{ inputs.package }}/transcript.json
        run: |
          if [ -f "$TRANSCRIPT" ]; then
            echo "::group::qualification transcript"
            cat "$TRANSCRIPT"
            echo "::endgroup::"
          else
            echo "no transcript file was written (the script failed before producing one -- see the step above for a thrown error)"
          fi

      # generate-qualification-record.mjs is the retention mechanism itself:
      # it re-derives every joined digest from the git tree at HEAD, proves
      # the tarball reproduces from a clean rebuild (issue #893), and REFUSES
      # (exit 1) if governance/release-qualifications/<recordStem>-<version>.json
      # already exists — the no-overwrite, one-introduction-per-path
      # retention this task requires, already enforced by the script this
      # workflow calls rather than re-implemented here.
      - name: Generate and retain qualification record
        id: record
        env:
          PKG: ${{ inputs.package }}
          TARBALL: ${{ runner.temp }}/qualification/${{ inputs.package }}/candidate.tgz
          TRANSCRIPT: ${{ runner.temp }}/qualification/${{ inputs.package }}/transcript.json
          RUN_ID: ${{ github.run_id }}
          RUN_ATTEMPT: ${{ github.run_attempt }}
        run: |
          set -euo pipefail
          reference="github-actions-dispatch:qualify-candidate.yml;run:${RUN_ID};attempt:${RUN_ATTEMPT}"
          node scripts/generate-qualification-record.mjs --package "$PKG" --tarball "$TARBALL" --transcript "$TRANSCRIPT" --review-reference "$reference"
          new_files="$(git status --porcelain -- governance/release-qualifications/ | awk '{print $2}')"
          count="$(printf '%s\n' "$new_files" | grep -c .)"
          if [ "$count" -ne 1 ]; then
            echo "::error title=Unexpected qualification record output::expected exactly one new file under governance/release-qualifications/, found $count."
            exit 1
          fi
          echo "record_path=$new_files" >> "$GITHUB_OUTPUT"

      # Cheap self-verification before spending an upload and a second job:
      # does the record this job just wrote actually join against the
      # candidate the way check-qualification-record-present.mjs (the same
      # join publish.yml's own discover job uses) says it should. This is an
      # early failure signal only -- this job is untrusted, and the
      # open-pull-request job re-validates the record from its own checkout.
      - name: Verify the retained record now matches the candidate
        env:
          PKG: ${{ inputs.package }}
        run: node scripts/check-qualification-record-present.mjs --package "$PKG"

      # The one product that crosses to the write job. Copied to a directory
      # of its own so the artifact's layout is exactly one file;
      # scripts/accept-qualification-handoff.mjs refuses anything else.
      - name: Stage qualification record for hand-off
        env:
          RECORD_PATH: ${{ steps.record.outputs.record_path }}
        run: |
          set -euo pipefail
          handoff="$RUNNER_TEMP/handoff"
          mkdir -p "$handoff"
          cp "$RECORD_PATH" "$handoff/qualification-record.json"

      - name: Upload qualification record
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7
        with:
          name: qualification-record-${{ github.run_attempt }}
          path: ${{ runner.temp }}/handoff/qualification-record.json
          if-no-files-found: error
          retention-days: 7

  open-pull-request:
    # Holds the write token and executes nothing the qualify job produced.
    # See the header's job-split section before adding any step here: no
    # npm ci/install, no package script, no build, no cache restore, and
    # the artifact is data for accept-qualification-handoff.mjs only.
    needs: qualify
    permissions:
      contents: write
      pull-requests: write
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      # A fresh checkout of the exact commit the dispatch ran on -- the same
      # commit the qualify job qualified and the record must name as its
      # reviewedCommit.
      - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4.4.0
        with:
          ref: ${{ github.sha }}
          fetch-depth: 0
          persist-credentials: false

      # Re-validated here rather than trusted from the qualify job: that
      # job's outputs are as untrusted as its artifact.
      - name: Validate package input
        env:
          PKG: ${{ inputs.package }}
        run: |
          if ! [[ "$PKG" =~ ^[a-zA-Z0-9_-]+$ ]]; then
            echo "::error title=Invalid package input::package must match ^[a-zA-Z0-9_-]+\$ (got: $PKG)"
            exit 1
          fi
          if [ ! -f "packages/$PKG/package.json" ]; then
            echo "::error title=Unknown package::packages/$PKG/package.json does not exist."
            exit 1
          fi

      # The pinned runtime only runs this repository's own scripts from the
      # fresh checkout. No npm command runs in this job.
      - uses: actions/setup-node@49933ea5288caeca8642d1e84afbd3f7d6820020 # v4.4.0
        with:
          node-version: 24.19.0

      - name: Assert pinned Node runtime
        run: test "$(node --version)" = 'v24.19.0'

      # Same derivation as the qualify job's own step, recomputed from this
      # job's trusted checkout instead of read from that job's outputs.
      - name: Resolve target version
        id: candidate
        env:
          PKG: ${{ inputs.package }}
          REQUESTED_VERSION: ${{ inputs.version }}
        run: |
          set -euo pipefail
          manifest_version="$(node -p "require('./packages/' + process.env.PKG + '/package.json').version")"
          if [ -n "$REQUESTED_VERSION" ] && [ "$REQUESTED_VERSION" != "$manifest_version" ]; then
            echo "::error title=Version mismatch::Requested version $REQUESTED_VERSION does not match packages/$PKG/package.json's current version $manifest_version."
            exit 1
          fi
          echo "version=$manifest_version" >> "$GITHUB_OUTPUT"

      # Downloaded OUTSIDE the workspace, into a directory nothing else
      # uses, and never executed or sourced. The name carries this run
      # attempt, matching the review reference the record must carry, so a
      # retry is "re-run all jobs": a re-run of this job alone finds no
      # artifact for its own attempt and fails closed.
      - name: Download qualification record
        uses: actions/download-artifact@37930b1c2abaa49bbe596cd826c3c89aef350131 # v7
        with:
          name: qualification-record-${{ github.run_attempt }}
          path: ${{ runner.temp }}/handoff

      - name: Accept qualification record hand-off
        id: record
        env:
          PKG: ${{ inputs.package }}
          VERSION: ${{ steps.candidate.outputs.version }}
          REVIEWED_COMMIT: ${{ github.sha }}
          RUN_ID: ${{ github.run_id }}
          RUN_ATTEMPT: ${{ github.run_attempt }}
        run: |
          set -euo pipefail
          reference="github-actions-dispatch:qualify-candidate.yml;run:${RUN_ID};attempt:${RUN_ATTEMPT}"
          record_path="$(node scripts/accept-qualification-handoff.mjs --package "$PKG" --version "$VERSION" --handoff-dir "$RUNNER_TEMP/handoff" --reviewed-commit "$REVIEWED_COMMIT" --review-reference "$reference")"
          echo "record_path=$record_path" >> "$GITHUB_OUTPUT"

      # Run here, from the trusted checkout, rather than carried across the
      # hand-off: no file removal is ever accepted from the qualify job.
      # A retained record makes the matching deferral file (if any) stale by
      # check-qualification-record-required.mjs's own `stale-deferral` rule
      # — removed here, in the SAME change, so this pull request does not
      # itself fail that gate and so no separate follow-up is needed to keep
      # governance/release-qualification-deferrals/ honest. A package qualified
      # with no prior deferral is not an error: the script no-ops.
      - name: Remove now-stale deferral entry, if any
        env:
          PKG: ${{ inputs.package }}
          VERSION: ${{ steps.candidate.outputs.version }}
        run: node scripts/remove-qualification-deferral.mjs --package "$PKG" --version "$VERSION"

      # The only changes this job may commit: the accepted record, and the
      # one deferral file for this package@version if it existed. Anything
      # else in the tree fails closed.
      - name: Verify the change set is exactly the record and its deferral
        env:
          PKG: ${{ inputs.package }}
          VERSION: ${{ steps.candidate.outputs.version }}
          RECORD_PATH: ${{ steps.record.outputs.record_path }}
        run: |
          set -euo pipefail
          deferral_path="governance/release-qualification-deferrals/${PKG}@${VERSION}.json"
          unexpected="$(git status --porcelain --untracked-files=all | awk -v record="?? $RECORD_PATH" -v deferral=" D $deferral_path" '$0 != record && $0 != deferral')"
          if [ -n "$unexpected" ]; then
            echo "::error title=Unexpected change set::only $RECORD_PATH and $deferral_path may change; found:"
            printf '%s\n' "$unexpected"
            exit 1
          fi
          git status --porcelain --untracked-files=all | grep -qxF "?? $RECORD_PATH"

      - name: Push branch and open pull request
        # The only step that sees the write token.
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          PKG: ${{ inputs.package }}
          VERSION: ${{ steps.candidate.outputs.version }}
          RECORD_PATH: ${{ steps.record.outputs.record_path }}
          GITHUB_BASE_REF_OR_DEFAULT: ${{ github.event.repository.default_branch }}
          RUN_ID: ${{ github.run_id }}
        run: |
          set -euo pipefail
          name="$(node -p "require('./packages/' + process.env.PKG + '/package.json').name")"
          branch="claude/qualify-${PKG}-${VERSION}"
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git checkout -b "$branch"

          # Two commits, not one. scripts/lib/candidate-qualification.mjs's
          # forward-record-paths rule requires a qualification-introduction
          # commit to change EXACTLY the qualification record path(s) it
          # introduces -- a deferral file under
          # governance/release-qualification-deferrals/ is not a record path
          # (that rule only ever recognizes paths under
          # governance/release-qualifications/), so it cannot share that commit,
          # the same as the old single governance/release-qualification-
          # deferrals.json file before it (#1254). A single "record + deferral"
          # commit (what this step used to make) passes every check that runs
          # before build-and-test, then fails build-and-test itself --
          # discovered when a consolidated batch of these records reached that
          # job for the first time. This mirrors the two-commit shape #1156
          # landed by hand for the same reason.
          record_message_file="$RUNNER_TEMP/qualify-candidate-commit-message.txt"
          {
            printf '%s\n\n' "Qualify ${name}@${VERSION} on the pinned release runtime"
            printf '%s\n' "Produced ${RECORD_PATH} by running scripts/run-candidate-qualification.mjs"
            printf '%s\n' "and scripts/generate-qualification-record.mjs on the exact runtime"
            printf '%s\n' "scripts/lib/release-runtime.mjs pins (Node v24.19.0, npm 11.17.0, zlib"
            printf '%s\n\n' "1.3.2.1-motley-3246f1b), via .github/workflows/qualify-candidate.yml."
            printf '%s\n\n' "This acknowledges qualification only; it does not publish ${name}@${VERSION}."
            printf '%s\n' "Ref: #948"
          } > "$record_message_file"
          git add "$RECORD_PATH"
          git commit -F "$record_message_file"

          deferral_path="governance/release-qualification-deferrals/${PKG}@${VERSION}.json"
          if ! git diff --quiet -- "$deferral_path"; then
            deferral_message_file="$RUNNER_TEMP/qualify-candidate-deferral-commit-message.txt"
            {
              printf '%s\n\n' "Remove the ${name}@${VERSION} deferral qualification made stale"
              printf '%s\n' "The record introduced in the previous commit makes ${deferral_path} stale"
              printf '%s\n' "by scripts/check-qualification-record-required.mjs's stale-deferral rule."
              printf '%s\n\n' "Separate commit from the record introduction: see that commit's own"
              printf '%s\n' "message for why a deferral file cannot share a commit with a"
              printf '%s\n\n' "qualification-record path."
              printf '%s\n' "Ref: #948"
            } > "$deferral_message_file"
            git add "$deferral_path"
            git commit -F "$deferral_message_file"
          fi

          git remote set-url origin "https://x-access-token:${GH_TOKEN}@github.com/${GITHUB_REPOSITORY}.git"
          git push origin "$branch"

          body_file="$RUNNER_TEMP/qualify-candidate-pr-body.txt"
          {
            printf '%s\n\n' "Qualifies \`${name}@${VERSION}\` on the exact release runtime \`scripts/lib/release-runtime.mjs\` pins (Node v24.19.0, npm 11.17.0, zlib 1.3.2.1-motley-3246f1b), produced by this workflow's own pinned \`actions/setup-node\` step — the same runtime \`ci.yml\`'s required \`build and test\` job already proves is available here."
            printf '%s\n' "- Retains \`${RECORD_PATH}\`, refusing to overwrite it if it already existed (\`scripts/generate-qualification-record.mjs\`)."
            printf '%s\n' "- Removes the matching deferral file under \`governance/release-qualification-deferrals/\`, if this version had one, so \`scripts/check-qualification-record-required.mjs\`'s \`stale-deferral\` rule does not fail on this change."
            printf '%s\n\n' "- Publishes nothing. A separate, manually dispatched \`publish.yml\` run is required to actually upload \`${name}@${VERSION}\`."
            printf '%s\n' "Run: ${GITHUB_SERVER_URL}/${GITHUB_REPOSITORY}/actions/runs/${RUN_ID}"
            printf '%s\n' "Ref: #948"
          } > "$body_file"

          # This repository does not permit GitHub Actions to create pull
          # requests (a repository setting this workflow does not, and
          # should not, change on its own) -- gh pr create fails here with
          # "GitHub Actions is not permitted to create or approve pull
          # requests". That is not this run's failure: the branch above is
          # already pushed with a complete, valid, retained record. Try PR
          # creation, and if it is refused for that specific reason, fall
          # back to a compare-URL summary instead of failing the whole run
          # -- a human (or a later run authorized to open PRs) can open it
          # from the branch. Any OTHER gh pr create failure still fails the
          # job; only this one, named reason degrades gracefully.
          set +e
          pr_create_output="$(gh pr create \
            --title "Qualify ${name}@${VERSION} on the pinned release runtime" \
            --base "$GITHUB_BASE_REF_OR_DEFAULT" \
            --head "$branch" \
            --body-file "$body_file" 2>&1)"
          pr_create_status=$?
          set -e

          if [ "$pr_create_status" -eq 0 ]; then
            echo "$pr_create_output"
          elif printf '%s' "$pr_create_output" | grep -q "not permitted to create or approve pull requests"; then
            {
              echo "### Qualification record produced, PR not opened"
              echo ""
              echo "\`${name}@${VERSION}\` was qualified and the record is retained on branch \`${branch}\`, but this repository does not permit GitHub Actions to open pull requests."
              echo ""
              echo "Open it from: ${GITHUB_SERVER_URL}/${GITHUB_REPOSITORY}/compare/${GITHUB_BASE_REF_OR_DEFAULT}...${branch}?expand=1"
            } >> "$GITHUB_STEP_SUMMARY"
            echo "PR creation was refused (Actions cannot create PRs on this repo); branch ${branch} is pushed and ready. See job summary for the compare URL."
          else
            echo "$pr_create_output" >&2
            exit "$pr_create_status"
          fi
