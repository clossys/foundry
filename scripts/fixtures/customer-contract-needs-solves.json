{
  "$comment": "Customer's contract-shaped `needs`, `solves` and `feeds` values, prepared in pull request #1398 and deferred there because the capability catalogue could not read them. Tests overlay this `foundry` block onto the real packages/customer/package.json to prove the catalogue, the offering-kits gate and Advisor accept docs/contracts/package-framework.json's shape. `needs` is the `keep-verdict` capability's own `inputs`; `solves` is `qualified` on the `keep-satisfied` case of a retained qualification record.",
  "role": "customer",
  "foundry": {
    "needs": [
      { "producerRole": "@clossys/strategist", "artifact": "audience-understanding" },
      { "producerRole": "@clossys/publisher", "artifact": "surface-documents" }
    ],
    "solves": [
      {
        "problem": "customer-would-they-keep-it",
        "statement": "We can't tell whether the person we made this for would actually keep it.",
        "metric": "customer keep rate",
        "proofCase": "keep-satisfied",
        "evidence": "qualified",
        "capability": "keep-verdict"
      }
    ],
    "feeds": [
      { "artifact": "keep-verdict", "path": "clossys/customer/keep.json" }
    ]
  }
}
